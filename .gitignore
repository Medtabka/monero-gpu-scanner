# Build artifacts
scan_cpu
scan_sub
gpu_scan
subaddr_table
keys_for_seed
keys_tool
gen_bench
gen_bench_sub
*.o
*.obj
*.exe

# Generated CUDA device code (regenerated by `make`)
device_crypto.inc

# Chain exports / fixtures / tables (can be huge; never commit)
*.bin
chain.bin
bench.bin
table.bin

# Wallet lists may contain keys — keep them local
wallets.txt

# Archives
*.tar.gz
*.tgz
*.zip

# Python
__pycache__/
*.pyc

# Editor / OS
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
